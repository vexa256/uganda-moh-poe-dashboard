<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera scanning (BCBP barcode, future MRZ/face/doc-scanner) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 2026-05-07: RECORD_AUDIO removed — voice dictation retired. -->

    <!-- Sentinel Model Manager network probe + Capacitor Network plugin -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Haptics -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Local notifications (WhatsApp-style alerts).
         POST_NOTIFICATIONS is mandatory from Android 13 / API 33; without
         it the OS silently drops every LocalNotifications.schedule() call
         and the user never sees an alert. SCHEDULE_EXACT_ALARM is required
         for follow-up due reminders that must fire at an exact wall-clock
         time. RECEIVE_BOOT_COMPLETED ensures pending notifications survive
         a device reboot. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Hardware features — declared optional so the APK installs on devices
         without a camera / autofocus and the app degrades gracefully via the
         Sentinel sandbox's isAvailable() probes. -->
    <uses-feature android:name="android.hardware.camera"           android:required="false" />
    <uses-feature android:name="android.hardware.camera.any"       android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <!-- 2026-05-07: microphone hardware feature removed — voice dictation retired. -->

    <!-- Package-visibility queries (Android 11+ / API 30+).
         Without these blocks the OS hides matching services from the app and
         the corresponding plugins silently report "not available":
           - SENDTO mailto:      required so the email-share fallback can
                                 resolve an installed mail client.
           - Browser VIEW:       lets share/openInBrowser resolve a browser. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
