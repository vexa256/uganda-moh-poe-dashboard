// Stub. Original (untracked) mrzMultilingual.js was lost. Returns null so
// the passport pipeline falls back to its existing parsers. Restore the
// real module from editor history for regional-ID OCR support.
export function parseRegionalIdFromOcr(_text) {
  return null;
}
